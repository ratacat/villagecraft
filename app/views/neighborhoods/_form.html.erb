<% 
   remote ||= false 
   neighborhood ||= @neighborhood
%>
<%= simple_form_for(neighborhood, :remote => remote, :html => {:class => remote ? 'remote_neighborhood' : nil}) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name, label: 'Name' %>
    <%= f.input :city, label: 'City' %>
    <%= f.input :state, label: 'State' %>
    <%= f.input :county, label: 'County' %>
    <%= f.input :_kml, as: :text, label: 'KML', input_html: {rows: 5, value: (neighborhood._kml || neighborhood.as_kml)} %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, :class => "btn-primary" %>
  </div>
<% end %>
