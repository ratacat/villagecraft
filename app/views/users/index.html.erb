<div class="page-header">
  <%= link_to new_admin_user_path, :class => 'btn btn-primary' do %>
    <i class="icon-plus icon-white"></i>
    New User
  <% end %>
  <h1>Listing users</h1>
</div>

<table class="table table-bordered table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th>Sign Ins</th>
      <th>Privileges</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
<% @users.each do |user| %>
    <tr>
      <td><%= link_to annotated_user_name(user), user %></td>
      <td><%= user.email %></td>
      <td><%= user.sign_in_count.to_s %></td>
      <td><%= user.privileges.join(', ') %></td>
      <td>
        <%= iconic_link_to 'Edit', edit_user_path(user), class: 'btn btn-mini', title: 'edit' %>
        <%= iconic_link_to 'Delete', user, confirm: 'Are you sure?', method: :delete, class: 'btn btn-mini btn-danger', title: 'delete' %>
      </td>
    </tr>
<% end %>
  </tbody>
</table>
