Hi <%= @user.name %>,

You are confirmed to attend <%= @event.title %>, hosted by <%= @event.host.name %>.

When
----

<%= strip_tags(render :partial => 'reruns/meeting_times_list', :locals => {:rerun => @event}) %>

Where
-----

<%= @event.venue.blank? ? 'TBD' : @event.location.address %>


Host
----

<%= @event.host.name %>
email: <%= @event.host.email %>
mobile #: <%= user_phone(@event.host) %> 
