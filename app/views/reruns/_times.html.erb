<%
  rerun ||= @rerun
%>
<%= render :partial => 'meetings/time', :locals => {:meeting => rerun.first_meeting, :short_date => true, :no_tz => true, :show_livestamp => true} %>
<% if rerun.meetings.count > 1 %>
  (first of <%= rerun.meetings.count %> meetings)
<% end %>
