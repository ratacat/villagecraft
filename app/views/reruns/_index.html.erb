<table class="table table-bordered table-striped">
  <tbody>
<% reruns.each do |rerun| %>
    <tr>
      <td><%= link_to rerun.title, rerun %></td>
      <td>
        <div class="blocky_spns">
          <%= render :partial => 'reruns/times', :locals => {:rerun => rerun} %>
        </div>
      </td>
    </tr>
<% end %>
  </tbody>
</table>
