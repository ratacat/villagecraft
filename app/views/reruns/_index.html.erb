<%
  show_links ||= false
  click_to_show ||= false
%>
<table class="table table-hover table-striped rerun_index">
  <tbody>
<% reruns.each do |rerun| %>
    <tr <% if click_to_show -%>
      class="click_to_show"
      href="<%= url_for(rerun) %>"
    <% end -%>>
      <%= meeting_time(rerun.first_meeting, :tag => :td, :short_date => true, :no_tz => true, :show_livestamp => false) %>
      <td><%= "#{rerun.meetings.count}x" if rerun.meetings.count > 1 %></td>
      <td><%= rerun.attendances.count %>/<%= rerun.max_attendees %> max</td>
      <td><%= inline_venue(rerun.first_meeting.venue) %></td>
      <td><%= rerun_price(rerun) %></td>
      <% if show_links %>
      <td>
        <%= link_to rerun, :class => '' do -%>
          <i class="icon-play"></i>
        <% end -%>
      </td>
      <% end %>
    </tr>
<% end %>
  </tbody>
</table>
