<%
  rerun ||= @rerun
  show_street = rerun.external? || rerun.open? || (not rerun.rsvp? or current_user.try(:attending_event?, rerun))
%>
<% if rerun.venue.blank? %>
  TBD
<% else %>
  <%= address(rerun.venue.location, wrapper_tag: :span, show_street: show_street, show_state: false) %>
<% end %>
